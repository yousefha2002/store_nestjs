export enum OrderStatus {
    PENDING_PAYMENT = 'pending_payment',
    PLACED = 'placed',
    PREPARING = 'preparing',
    READY = 'ready',
    ARRIVED = 'arrived',
    RECEIVED = 'received',
    CANCELED = 'canceled',
}